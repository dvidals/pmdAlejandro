<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ej_spinner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/EjSpinner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Spinner6Activity" />
        <activity android:name=".Spinner5Activity" />
        <activity android:name=".Spinner4Activity" />
        <activity android:name=".Spinner3Activity" />
        <activity android:name=".Spinner2Activity" />
        <activity android:name=".Spinner1Activity" />
    </application>

</manifest>